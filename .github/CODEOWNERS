# Ownership of this branch.
#
# GitHub reads CODEOWNERS from the *base* branch of a pull request, so this file
# has to exist on every protected branch it should apply to — a copy on main
# alone would not affect pull requests targeting develop.
#
# Paired with require_code_owner_review in the branch ruleset, this means every
# pull request into a protected branch needs an approving review from the owner
# below. The owner is a bypass actor, so his own pull requests are exempt —
# GitHub does not allow approving your own PR, and the bypass resolves that.

* @ikostis-ucl
